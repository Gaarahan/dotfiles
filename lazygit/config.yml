gui:
  language: 'en'
  # Use delta's own side-by-side for modified files; keep lazygit diff view single-pane.
  splitDiff: never
keybinding:
  universal:
    filteringMenu: '<c-f>'
git:
  pagers:
    - colorArg: always
      pager: lazygit-delta-pager
